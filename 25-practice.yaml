- name: print the variables
  hosts: web
  var_files:
  - vars.yaml

  tasks:
  - name: print the string
    ansible.builtin.debug:
      msg: "Hi i am {{ Name }}, my experiance is {{ EXPERIANCE }} and my role is {{ ROLE }}"
  