- name: install the given softwares
  hosts: web
  become: yes
  tasks:
  - name:
    ansible.builtin.package:
      name: "{{ item.name }}"
      state: "{{ item.present }}"
    
    loop:
    - { name: 'mysql', state: 'present' }
    - { name: 'nodejs', state: 'absent' }