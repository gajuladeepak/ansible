- name: create User
  hosts: web
  become: yes
  tasks:
  - name: check user exist or not
    ansible.builtin.command: id expense
    register: USER

  - name: print the USER
    ansible.builtin.debug:
      msg: "{{ USER }}"